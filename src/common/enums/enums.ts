export enum InvitationStatus {
  PENDING = 'pending',
  ACCEPTED = 'accepted',
}

export enum NotificationStatus {
  UNREAD = 'unread',
  READ = 'read',
}

export enum Role {
  USER = 'user',
  ADMIN = 'admin',
}

export enum Permission {
  READ = 'read',
  EDIT = 'edit',
  ADMIN = 'admin',
}
