# ─────────── ОБЩЕЕ ───────────
NODE_ENV=test
PORT=5001                 # тестовое приложение можно поднять на другом порту

# ─────────── POSTGRES (Docker) ───────────
PG_HOST=localhost
PG_PORT=5433              # из docker-compose.test.yml
PG_USER=test
PG_PASS=test
PG_DB=myapp_test

# ─────────── JWT (можно фиктивные) ───────
JWT_ACCESS_SECRET=test_access_secret
JWT_REFRESH_SECRET=test_refresh_secret

# ─────────── Внешние сервисы — заглушки ──
FRONTEND_URL=http://localhost:3000
OPENAI_API_KEY=dummy-key          # чтобы openai не падал
SMTP_HOST=localhost
SMTP_PORT=2525
SMTP_USER=dummy
SMTP_PASSWORD=dummy
GOOGLE_CLIENT_ID=dummy
GOOGLE_CLIENT_SECRET=dummy
